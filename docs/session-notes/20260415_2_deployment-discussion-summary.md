# MeshFree 部署准备讨论超短总结

## 日期

- `2026-04-15`

## 本轮讨论做了什么

- 明确了服务器部署路线：`Ubuntu 22.04 + Node.js 20 + PM2 + Nginx + SQLite`
- 明确了域名与 HTTPS 方案：主域名 `yukiho.site`，`www` 跳转到裸域名，证书使用 `Let's Encrypt`
- 明确了登录与权限策略：首次用 `root` 初始化，后续使用 `koito + sudo`，禁用 `root` 远程登录
- 明确了管理员初始化方案：首次使用 seed，后续用专用脚本维护管理员账号
- 补齐了部署所需文档、环境变量模板、PM2 配置、Nginx 模板和管理员维护脚本

## 当前阶段结论

项目已经从“本地三条核心闭环已打通”，推进到“部署准备文档和基础运维辅助文件已基本齐备”的状态。
